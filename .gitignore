# Dependencies
/node_modules
/.pnp
.pnp.js
.pnp.cjs
# Lock files – keep for reproducible installs (uncomment to ignore)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Next.js
/.next/
/out/
.next/
out/

# Production
/build

# Testing
/coverage

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Local env files
.env
.env.*
!.env.example

# Vercel
.vercel

# Turbo / Turbopack
.turbo

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Generated / build artifacts
src/assets/iconify-icons/generated-icons.js
src/assets/iconify-icons/generated-icons.css

# Prisma
# (schema.prisma is usually committed; uncomment if you use local DB files)
# prisma/*.db
# prisma/*.db-journal

# Misc
.DS_Store
*.pem
Thumbs.db

# IDE / editor
.vscode/
.idea/
*.sublime-*

# Lint / style caches
.eslintcache
.stylelintcache

# Optional
*.log
.cache/

# Readme
README.md
readme.md
README

# -----------------------------------------------------------------------
# Kegal360 Admin Panel only – exclude template/demo pages (do not push to Render)
# Keep: admin/*, login, register, forgot-password, layout, api/auth
# -----------------------------------------------------------------------

# Template auth demo pages (v1, v2, multi-steps, etc.) – not used
src/app/\[lang\]/(blank-layout-pages)/pages/

# Template dashboard apps – not used (we only use admin/*)
src/app/\[lang\]/(dashboard)/(private)/apps/

# Template charts, dashboards, forms, react-table – not used
src/app/\[lang\]/(dashboard)/(private)/charts/
src/app/\[lang\]/(dashboard)/(private)/dashboards/
src/app/\[lang\]/(dashboard)/(private)/forms/
src/app/\[lang\]/(dashboard)/(private)/react-table/

# Template pages (account-settings, faq, pricing, widget-examples, etc.) – not used
src/app/\[lang\]/(dashboard)/(private)/pages/

# Front-pages (landing, pricing, help-center, checkout, payment) – not used in admin
src/app/front-pages/

# Mock API routes for template apps – not used
src/app/api/apps/
src/app/api/pages/

# Template views (apps, charts, dashboards, forms, front-pages, react-table, pages) – not used
src/views/apps/
src/views/charts/
src/views/dashboards/
src/views/forms/
src/views/front-pages/
src/views/react-table/
src/views/pages/

# -----------------------------------------------------------------------
# More template/demo files – not needed for Kegal360 admin
# -----------------------------------------------------------------------

# Mock data for template apps (academy, ecommerce, invoice, etc.)
src/fake-db/

# One-time migration/translation scripts – not needed at runtime
src/remove-translation-scripts/

# Template public images (demo apps, front-pages, auth masks, illustrations, cards, logos)
public/images/apps/
public/images/front-pages/
public/images/pages/
public/images/illustrations/
public/images/cards/
public/images/logos/

# Local Prisma DB (use MongoDB in production)
src/prisma/dev.db
src/prisma/migrations/

# Redux template slices (calendar, chat, email, kanban) – not used in admin
src/redux-store/slices/

# Extra lockfile (use npm only to avoid “multiple lockfiles” warning)
pnpm-lock.yaml
